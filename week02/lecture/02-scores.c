#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int scores[3]; // declara que o vetor terá 3 espaços na memória

    for()
    {
        
    }
    printf("Average: %f\n", (scores[0] + scores[1] + scores[2]) / 3.0);
}
