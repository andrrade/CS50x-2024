#include <cs50.h>
#include <stdio.h>

int main(void){
    do{
        long number = get_long("Number: ");
    } while (number <= 0);

    if(number.lenght() < 13){
        printf("INVALID");
    }
    else{
        if(number.lenght() >= 13 && <= 16){
            if(number starts == 34 || 37){

            }
        }
        else{
            if(number.lenght() == 16){
                if(number starts >= 51 && <= 55){

            }
            }
            else{
                if(number.lenght() == 15){
                    if(number starts == 4);
                }
                else{
                    printf("INVALID")
                }
            }
        }
    }
}
