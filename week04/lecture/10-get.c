#include <stdio.h>

int main(void)
{
    // get_int:
    // int n;
    // printf("n: ");
    // scanf("%i", &n);
    // printf("n: %i\n", n);

    // get_string
    char *s; // char * is already an adress and don't need ampersand (&), like integers.
    printf("s: ");
    scanf("%s", s);
    printf("s: %s\n", s)
}
